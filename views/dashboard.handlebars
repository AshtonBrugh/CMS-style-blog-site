 
<section class="new-post-container">
    <h2 class="new-post-container-title">Create New Post</h2>
    <form class="new-post">
      <div class="title-content">
        <div>
            <label class="post-title" for="post-title">Title:</label>
            <input type="text" id="post-title" name= "post-title">
        </div>
        <div>
            <label class="post-title" for="post-content">Content:</label>
            <textarea id="post-content" name="post-content"</textarea></textarea>
            </div>
        </div>
        <button id="create-btn" type="submit" class="button">Create</button>
    </form>
</section>

{{#if posts.length}}
<section class="your-posts">
  <ul class="ul-your-post">
    <h2 class="your-posts-title">Your Posts</h2>
    {{#each posts as |post|}}
    <li>
      {{> post-info post}}
      <a href="/dashboard/edit/{{post.id}}" class="edit-link">Edit post</a>
    </li>
    {{/each}}
  </ul>
</section>
{{/if}}


<script src="/javascript/add-post.js"></script>