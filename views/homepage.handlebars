<ol class="post-list">
    <li>
        <article class="post">
            <div class="title">
                <span>{{title}}</span>
                </div>
                <div class="meta">
                    Post by {{user.username}} on {{created_at}}
                    |
                <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
                </div>
        </article>
    </li>