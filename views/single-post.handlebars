{{> post-info post}}

{{#if loggedIn}}
<form class="comment-form">
    <div>
        <textarea rows="8" cols="40"name="comment-body"></textarea>
    </div>

    <div>
        <button class="comment-btn" type="submit">add comment</button>
    </div>
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}